# Dev only. `craco start` serves the client on :3000; the game server runs
# separately on :8080 (`npm run dev:server`). In production both are one origin
# and this var is unset, so the client falls back to its own host.
REACT_APP_WS_URL=ws://localhost:8080
