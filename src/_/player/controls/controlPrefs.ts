export enum CONTROL_PREFS {
  FORWARD = "KeyW",
  BACKWARD = "KeyS",
  LEFT = "KeyA",
  RIGHT = "KeyD",
  SPRINT = "ShiftLeft",
  JUMP = "Space",
}
//TODO hook this up (or something similar) to allow for variable controls
