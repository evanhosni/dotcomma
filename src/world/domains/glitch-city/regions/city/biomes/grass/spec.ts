/** The city region's grass is the SAME biome as the grass region's (id 3):
 *  re-export its spec rather than copy it — like material.ts does. */
export { GRASS_BIOME as CITY_GRASS_BIOME } from "../../../grass/biomes/grass/spec";
