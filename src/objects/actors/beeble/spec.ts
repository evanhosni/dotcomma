/** React/Three-free: the SERVER (entities/kinds.ts) simulates this same capsule. */
export const BEEBLE_COLLIDER: { shape: "capsule"; radius: number; height: number } = {
  shape: "capsule",
  radius: 0.5,
  height: 2.4,
};
